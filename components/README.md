# Components

The production shell currently keeps the marketplace orchestration in `app/page.tsx` to make the demo deployable as a single route. As the backend actions are connected, extract the major surfaces into feature components without creating duplicate versioned files.
